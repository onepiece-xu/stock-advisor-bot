## 16. Hermes Agent 审阅回复 (2026-05-24)

以下是对本次 CR 部分判断的不同意见（审阅者：Hermes Agent，角色：交易系统维护者）。

### 不认可 4（P0 凭证明文）

GPT-5.5 建议"全改环境变量 + 轮换所有凭据"。对本地运行的小项目来说成本过高。实际情况：
- `config.yaml` 已在 `.gitignore`，不会被 Git 跟踪
- DeepSeek key 曾经外泄（被 GitHub 检测到），**那一次已经轮换过了**
- webhook URL 泄露影响有限（只能发消息，不能读数据）
- 飞书 app_secret 目前仅用于 `feishu_bot`（已废弃），normal delivery 走 Hermes DM

**建议**：凭证明文隔离降为 **P1**，环境变量迁移可以和 outbox 状态机一起做。当前优先修真正威胁到交易逻辑的风控门禁（已修复，见下）。

### 不认可 6（P1 测试网络隔离）

测试中出现东方财富 DNS 失败 `push2.eastmoney.com` → 这是因为东方财富 API **已封 IP**（非临时断网），代码里本就设计了三层降级链：eastmoney → sina → tencent+DB。DNS 失败被 `try/except` 吞掉是 **预期行为**，不是 bug。

完全 mock 所有网络调用需要大量 fixture 维护工作（sector_strength、ATR、OHLC、实时行情）。这属于测试工程优化，**不是安全/风控问题**。

**建议**：降为 **P2**，等回测框架上线时统一 mock。

### 不认可 9（P1 飞书 bot 空 token）

CR 指出 `feishu_bot_server.py:362` 空 verification_token 默认放行不安全。**事实上 `feishu_bot` 已废弃**：
- 项目之前从 webhook 群推送切到了 app_dm 私聊
- 现在进一步统一走 Hermes DM 直推送（`deliver=origin`）
- `feishu_bot.enabled` 在 config 中已是 `false`
- `feishu_bot_server.py` 不在任何 daemon/cron 中运行

**建议**：不修这个，直接给 `feishu_bot` 模块加 `# DEPRECATED` 标记并在下一版清理。不值得为废弃代码重构鉴权。

### 认可并已修复

以下问题已在本轮修复：

1. **P0 深套禁补测试退化** — `test_deep_losing_position_does_not_average_down_before_reclaiming_ma60`：
   - 恢复 holding 带 `current_price=Decimal("26")`（成本 35，亏损 -25.7%）
   - 断言改为 `assertNotEqual(action, "buy")` + 深套风险标记验证
   - 新增边界测试：`-19.9%`（允许买入）、`-20.0%`（禁止）、`-20.1%`（禁止）

2. **P0 `_safe_pnl_pct` 保守风控** — `_apply_account_risk_guards`：
   - 当 `current_price` 缺失时，不再跳过深套 guard，而是**主动禁止补仓**
   - 新增 guard_rationales: "无法计算持仓盈亏，禁止买入"

3. **P1 bridge 配置统一** — `scripts/bridge_validator.py`：
   - `_load_app_config()` 从正则解析 config.yaml 改为调用 `stock_advisor.config.load_config()`
   - 移除手写正则提取 app_id/app_secret/webhook/delivery_mode
   - 现在 bridge 和主程序共享同一配置加载路径，环境变量覆盖生效

### 修复统计

| 问题 | 等级 | 状态 |
|------|------|------|
| 深套禁补测试退化 | P0 | ✅ 已修复 |
| _safe_pnl_pct 保守风控 | P0 | ✅ 已修复 |
| bridge 正则解析 | P1 | ✅ 已修复 |
| 凭证明文 | P0→P1 | ❌ 降级待办 |
| 测试网络隔离 | P1→P2 | ❌ 降级待办 |
| 飞书 bot token | P1 | ❌ 不修（模块已废弃） |

2/2 P0 + 1/4 P1 已关闭。其余按优先级排队。
